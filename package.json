{
  "name": "resume",
  "version": "1.0.0",
  "scripts": {
    "build": "npx resumed render resume.json --theme jsonresume-theme-macchiato -o index.html && node inject-pdf-link.mjs",
    "pdf": "node generate-pdf.mjs",
    "dev": "npm run build && open index.html"
  },
  "license": "MIT",
  "dependencies": {
    "jsonresume-theme-macchiato": "^1.1.0",
    "resumed": "^6.1.0"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "puppeteer": "^24.38.0",
    "resume-cli": "^3.0.8",
    "semantic-release": "^24.0.0"
  }
}
