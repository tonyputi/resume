{
  "name": "resume",
  "version": "1.0.0",
  "scripts": {
    "build": "npx resumed render resume.json --theme jsonresume-theme-macchiato -o index.html",
    "dev": "npm run build && open index.html"
  },
  "license": "MIT",
  "dependencies": {
    "jsonresume-theme-macchiato": "^1.1.0",
    "resumed": "^6.1.0"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "^24.0.0"
  }
}
